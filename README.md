# CSDS Project

Python scripts for CSDS parameter extraction, cleaning, modeling, plotting, and polynomial fitting.

## Setup

python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
